{
  "name": "mrtki-block-reloaded",
  "version": "1.0.0",
  "description": "blokování hnoje na webu",
  "main": "main.js",
  "scripts": {
    "build": "node scripts/clean.js && node build.mjs && node scripts/build.js",
    "install-deps": "npm install",
    "webext-lint": "npm run build && web-ext lint --source-dir dist",
    "webext-start": "npm run build && web-ext run --source-dir dist --start-url /home/dlabaja/PhpstormProjects/MrtkiBlockReloaded/tests/test-file.html --devtools --verbose"
  },
  "type": "module",
  "author": "dlabaja",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^25.6.2",
    "@types/webextension-polyfill": "^0.12.5",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "webextension-polyfill": "^0.12.0"
  }
}
